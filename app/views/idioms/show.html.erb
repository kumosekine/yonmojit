<div class="contents row">
      <span class="name"><%= @idiom.title %>
    </span>
       <p><%= @idiom.text %>
    </p>
    <p class="day_time"><%= @idiom.updated_at.strftime("%Y年%m月%d日 %H:%M:%S") %></p>
    <%= link_to '削除', idiom_path(@idiom.id), method: :delete %>
</div>