<div class="contents row">
  <div class="container">
  <h3>投稿する</h3>
  <%= form_with model:@idiom, local: true do |form| %>
  <%= form.text_field :title, placeholder: "四文字" %>
  <%= form.text_area :text, placeholder: "必須では有りません", rows: "10" %>
  <%= form.submit "投稿" %>
  <% end %>
  </div>
  </div>